# Because WordPress is SVN and this is git
.svn

# Because this really is a working directory, ignore vagrant's files
/.vagrant

# Enables local modifications
wp-cli.local.yml

# Don't include node modules or downloaded sql files
VipSyncServer/node_modules
VIPSyncServer/npm-debug.log
